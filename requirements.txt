# ── Core Web Framework ──────────────────────────────────────────────────────
fastapi>=0.138.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9         # Form data parsing

# ── Database & ORM ───────────────────────────────────────────────────────────
sqlalchemy>=2.0.28
alembic>=1.13.1
aiosqlite>=0.20.0               # SQLite async driver (dev / hackathon)
asyncpg>=0.29.0                 # PostgreSQL async driver (production)

# ── Validation & Settings ────────────────────────────────────────────────────
pydantic>=2.6.4
pydantic-settings>=2.2.1
email-validator>=2.1.1

# ── Authentication ───────────────────────────────────────────────────────────
bcrypt>=4.0.0                   # Password hashing (Python 3.14 compatible)
pyjwt[crypto]>=2.8.0            # JWT tokens

# ── Templating ───────────────────────────────────────────────────────────────
jinja2>=3.1.3

# ── HTTP Client ──────────────────────────────────────────────────────────────
httpx>=0.27.0                   # Async HTTP for LLM provider calls

# ── Cache & Message Broker ───────────────────────────────────────────────────
redis>=5.0.3

# ── Configuration ────────────────────────────────────────────────────────────
python-dotenv>=1.0.1

# ── Logging ──────────────────────────────────────────────────────────────────
python-json-logger>=2.0.7

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.1.1
pytest-asyncio>=0.23.5
anyio>=4.0.0
